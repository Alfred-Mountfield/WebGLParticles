import {Color, ShaderMaterial} from "three";
import {parameters} from "../guiParameters";

export function addFragmentUniforms(renderMaterial: ShaderMaterial) {
    // const color1 = new Color("#1f93c9")
    // const color2 = new Color("#1fc82e")
    //
    // const uniforms = renderMaterial.uniforms
    // uniforms["lightPos"] = {value: new Float32Array([0, 190, 0])}
    // uniforms["color1"] = {value: color1}
    // uniforms["color2"] = {value: color2}
    // uniforms["maxValue"] = {value: parameters["Maximum Value"]}
}
